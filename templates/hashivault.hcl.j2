listener "tcp" {
  address          = "{{ vault_listen_address }}"
  tls_disable = "true"
}

storage "dynamodb" {
  table = "{{ dynamodb_table_name }}"
  region     = "{{ aws_region }}"
}

api_addr = "http://{{ vault_listen_address }}"